version: 2.1
jobs:
  first_job:
    docker:
      - image: circleci/django:latest
    steps:
      - checkout 
      - run: echo "trying to understand this"
  
  second_job:
    steps:
      - checkout
      - run: echo "that one who called binga"
